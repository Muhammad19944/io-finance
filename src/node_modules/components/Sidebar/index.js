
import React from 'react'
import {Link} from 'react-router-dom'
import {Button} from 'antd'
import routes from 'routes'

import Logo from 'assets/img/logo.png'
import './index.css'

const AppSidebar = () => {
	return (
		<div className='sidebar-inner'>
			<Link to='/' className='sidebar-header d-flex align-center justify-center'>
				<img src={Logo} alt="App Logo" />
			</Link>

			<ul className='sidebar-content'>
				{routes.map((route) => (
					<li key={route.name}>
						<Link
							to={route.path}
							className='d-flex align-center p-relative'
						>{route.name}</Link>
					</li>
				))}
			</ul>

			<ul className='sidebar-bottom'>
				<li>
					<Link to='/help' className='d-flex align-center p-relative'>
						Help Center
					</Link>
				</li>
				<li>
					<Link to='/contact' className='d-flex align-center p-relative'>
						Contact Us
					</Link>
				</li>
				<li>
					<Button type="link" danger size='large'>
						Log Out
					</Button>
				</li>
			</ul>
		</div>
	)
}

export default AppSidebar